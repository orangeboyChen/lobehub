diff --git a/apps/server/src/routers/lambda/recent.ts b/apps/server/src/routers/lambda/recent.ts
index 7042af85c5..aa7da30d80 100644
--- a/apps/server/src/routers/lambda/recent.ts
+++ b/apps/server/src/routers/lambda/recent.ts
@@ -13,0 +14 @@ export interface RecentItem {
+  groupId?: string | null;
@@ -88,0 +90 @@ export const recentRouter = router({
+          groupId: item.groupId,
diff --git a/packages/database/src/models/__tests__/recent.test.ts b/packages/database/src/models/__tests__/recent.test.ts
index c5274ca041..a149e4c404 100644
--- a/packages/database/src/models/__tests__/recent.test.ts
+++ b/packages/database/src/models/__tests__/recent.test.ts
@@ -103,0 +104 @@ describe('RecentModel', () => {
+          groupId: 'group-1',
diff --git a/packages/database/src/models/recent.ts b/packages/database/src/models/recent.ts
index 6823c1db1d..b8e05ff566 100644
--- a/packages/database/src/models/recent.ts
+++ b/packages/database/src/models/recent.ts
@@ -11,0 +12 @@ export interface RecentDbItem {
+  groupId: string | null;
@@ -86,0 +88 @@ export class RecentModel {
+        groupId: sql<string | null>`${topics.groupId}`.as('group_id'),
@@ -117,0 +120 @@ export class RecentModel {
+        groupId: sql<string | null>`NULL`.as('group_id'),
@@ -146,0 +150 @@ export class RecentModel {
+        groupId: sql<string | null>`NULL`.as('group_id'),
@@ -182,0 +187 @@ export class RecentModel {
+        groupId: row.groupId,
diff --git a/src/features/AgentTasks/shared/useAgentDisplayMeta.ts b/src/features/AgentTasks/shared/useAgentDisplayMeta.ts
index 918829beec..5f1d02a96a 100644
--- a/src/features/AgentTasks/shared/useAgentDisplayMeta.ts
+++ b/src/features/AgentTasks/shared/useAgentDisplayMeta.ts
@@ -8,0 +9 @@ import { agentSelectors, builtinAgentSelectors } from '@/store/agent/selectors';
+import { useAgentGroupStore } from '@/store/agentGroup';
@@ -37,0 +39,7 @@ export const useAgentDisplayMeta = (
+  const groupBySupervisorId = useAgentGroupStore((s) =>
+    agentId
+      ? Object.values(s.groupMap).find(
+          (group) => group.supervisorAgentId === agentId && !!group.avatar,
+        )
+      : undefined,
+  );
@@ -45,0 +54 @@ export const useAgentDisplayMeta = (
+    !!groupBySupervisorId?.avatar ||
@@ -54 +63,5 @@ export const useAgentDisplayMeta = (
-    avatar: meta?.avatar || sidebarAvatar || (isInbox ? DEFAULT_INBOX_AVATAR : DEFAULT_AVATAR),
+    avatar:
+      groupBySupervisorId?.avatar ||
+      meta?.avatar ||
+      sidebarAvatar ||
+      (isInbox ? DEFAULT_INBOX_AVATAR : DEFAULT_AVATAR),
@@ -56 +69,4 @@ export const useAgentDisplayMeta = (
-      meta?.backgroundColor || sidebarAgent?.backgroundColor || cssVar.colorBgContainer,
+      groupBySupervisorId?.backgroundColor ||
+      meta?.backgroundColor ||
+      sidebarAgent?.backgroundColor ||
+      cssVar.colorBgContainer,
@@ -57,0 +74 @@ export const useAgentDisplayMeta = (
+      groupBySupervisorId?.title ||
diff --git a/src/features/Home/HomeModeContent.tsx b/src/features/Home/HomeModeContent.tsx
index 0ab7d986bf..a22bff6db9 100644
--- a/src/features/Home/HomeModeContent.tsx
+++ b/src/features/Home/HomeModeContent.tsx
@@ -30,0 +31 @@ import { recentService } from '@/services/recent';
+import { useAgentGroupStore } from '@/store/agentGroup';
@@ -202,0 +204,5 @@ const RecentTopicRow = memo<{ showAuthor?: boolean; topic: RecentItem }>(
+    const group = useAgentGroupStore((s) =>
+      topic.groupId ? s.groupMap[topic.groupId] : undefined,
+    );
+    const avatar = group?.avatar || agent?.avatar;
+    const backgroundColor = group?.backgroundColor || agent?.backgroundColor;
@@ -217 +223 @@ const RecentTopicRow = memo<{ showAuthor?: boolean; topic: RecentItem }>(
-          agent ? (
+          avatar ? (
@@ -219,2 +225,2 @@ const RecentTopicRow = memo<{ showAuthor?: boolean; topic: RecentItem }>(
-              avatar={agent.avatar}
-              background={agent.backgroundColor}
+              avatar={avatar}
+              background={backgroundColor}
@@ -478,0 +485,2 @@ const HomeModeContent = memo<HomeModeContentProps>(({ inlineRail, mode, onSugges
+  const useFetchGroups = useAgentGroupStore((s) => s.useFetchGroups);
+  useFetchGroups(Boolean(isLogin && !recentsHidden), Boolean(isLogin));
diff --git a/src/features/Home/Recents/Item.tsx b/src/features/Home/Recents/Item.tsx
index 8ccb905742..5b3ec91422 100644
--- a/src/features/Home/Recents/Item.tsx
+++ b/src/features/Home/Recents/Item.tsx
@@ -1 +1 @@
-import { ActionIcon, DropdownMenu, Flexbox, Icon } from '@lobehub/ui';
+import { ActionIcon, Avatar, DropdownMenu, Flexbox, Icon } from '@lobehub/ui';
@@ -13,0 +14 @@ import { type RecentItem } from '@/server/routers/lambda/recent';
+import { useAgentGroupStore } from '@/store/agentGroup';
@@ -23,0 +25 @@ const RecentListItem = memo<RecentItem>((item) => {
+  const group = useAgentGroupStore((s) => (item.groupId ? s.groupMap[item.groupId] : undefined));
@@ -73,0 +76,10 @@ const RecentListItem = memo<RecentItem>((item) => {
+          if (type === 'topic' && group?.avatar) {
+            return (
+              <Avatar
+                avatar={group.avatar}
+                background={group.backgroundColor || undefined}
+                shape={'circle'}
+                size={20}
+              />
+            );
+          }
diff --git a/src/features/Portal/GroupThread/Header/index.tsx b/src/features/Portal/GroupThread/Header/index.tsx
index 2bfefbb0f7..c4f8c368fb 100644
--- a/src/features/Portal/GroupThread/Header/index.tsx
+++ b/src/features/Portal/GroupThread/Header/index.tsx
@@ -21,0 +22,3 @@ const Header = memo(() => {
+  const groupMeta = useAgentGroupStore((s) =>
+    s.activeGroupId ? s.groupMap[s.activeGroupId] : undefined,
+  );
@@ -24,0 +28 @@ const Header = memo(() => {
+  const isSupervisor = groupMeta?.supervisorAgentId === currentAgent?.id;
@@ -34,2 +37,0 @@ const Header = memo(() => {
-            avatar={currentAgent?.avatar || DEFAULT_AVATAR}
-            background={currentAgent?.backgroundColor ?? undefined}
@@ -37,0 +40,10 @@ const Header = memo(() => {
+            avatar={
+              isSupervisor
+                ? groupMeta?.avatar || DEFAULT_AVATAR
+                : currentAgent?.avatar || DEFAULT_AVATAR
+            }
+            background={
+              isSupervisor
+                ? (groupMeta?.backgroundColor ?? undefined)
+                : (currentAgent?.backgroundColor ?? undefined)
+            }
diff --git a/src/routes/(main)/group/profile/features/Header/index.tsx b/src/routes/(main)/group/profile/features/Header/index.tsx
index 98238da485..1be873456a 100644
--- a/src/routes/(main)/group/profile/features/Header/index.tsx
+++ b/src/routes/(main)/group/profile/features/Header/index.tsx
@@ -58,0 +59 @@ const Header = memo(() => {
+  const groupMeta = useAgentGroupStore((s) => agentGroupSelectors.getGroupMeta(gid ?? '')(s));
@@ -84,2 +85,2 @@ const Header = memo(() => {
-        avatar: agent.isSupervisor ? undefined : agent.avatar || undefined,
-        icon: agent.isSupervisor ? <Crown size={16} /> : undefined,
+        avatar: agent.isSupervisor ? groupMeta.avatar : agent.avatar || undefined,
+        icon: agent.isSupervisor && !groupMeta.avatar ? <Crown size={16} /> : undefined,
@@ -93 +94 @@ const Header = memo(() => {
-  }, [members, t]);
+  }, [groupMeta.avatar, members, t]);
diff --git a/src/store/session/slices/session/action.ts b/src/store/session/slices/session/action.ts
index f899867db8..76ff21c920 100644
--- a/src/store/session/slices/session/action.ts
+++ b/src/store/session/slices/session/action.ts
@@ -231,2 +231,2 @@ export class SessionActionImpl {
-              avatar: null,
-              backgroundColor: null,
+              avatar: session.meta?.avatar ?? null,
+              backgroundColor: session.meta?.backgroundColor ?? null,
