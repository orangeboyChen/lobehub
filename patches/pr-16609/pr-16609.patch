diff --git a/src/features/Connectors/ConnectorDetail/index.test.tsx b/src/features/Connectors/ConnectorDetail/index.test.tsx
index da4e156d..30aa29e1 100644
--- a/src/features/Connectors/ConnectorDetail/index.test.tsx
+++ b/src/features/Connectors/ConnectorDetail/index.test.tsx
@@ -12,0 +13 @@ const mocks = vi.hoisted(() => ({
+  headerWidth: 1024,
@@ -41,0 +43,4 @@ const mocks = vi.hoisted(() => ({
+vi.mock('ahooks', () => ({
+  useSize: () => ({ height: 42, width: mocks.headerWidth }),
+}));
+
@@ -71,0 +77 @@ vi.mock('@lobehub/ui/base-ui', () => ({
+    'aria-label': ariaLabel,
@@ -73,0 +80 @@ vi.mock('@lobehub/ui/base-ui', () => ({
+    title,
@@ -75,0 +83 @@ vi.mock('@lobehub/ui/base-ui', () => ({
+    'aria-label'?: string;
@@ -77,0 +86 @@ vi.mock('@lobehub/ui/base-ui', () => ({
+    title?: string;
@@ -80 +89 @@ vi.mock('@lobehub/ui/base-ui', () => ({
-    <button disabled={disabled} type="button" onClick={onClick}>
+    <button aria-label={ariaLabel} disabled={disabled} title={title} type="button" onClick={onClick}>
@@ -114,0 +124 @@ describe('ConnectorDetail', () => {
+    mocks.headerWidth = 1024;
@@ -151,0 +162,25 @@ describe('ConnectorDetail', () => {
+
+  it('collapses icon buttons when the header is narrow', () => {
+    mocks.headerWidth = 500;
+    mocks.toolState.connectors = [
+      {
+        id: 'connector-1',
+        identifier: 'custom-id',
+        metadata: { description: 'Workspace notes' },
+        mcpConnectionType: 'http',
+        name: 'Custom',
+        sourceType: ConnectorSourceType.custom,
+      },
+    ];
+
+    render(<ConnectorDetail connectorId="connector-1" />);
+
+    expect(screen.getByText('Custom')).toBeInTheDocument();
+    expect(screen.getByRole('button', { name: 'Reset permissions' })).toBeInTheDocument();
+    expect(screen.queryByText('Sync')).not.toBeInTheDocument();
+    expect(screen.queryByText('Edit')).not.toBeInTheDocument();
+    expect(screen.queryByText('Delete')).not.toBeInTheDocument();
+    expect(screen.getByRole('button', { name: 'Sync' })).toBeInTheDocument();
+    expect(screen.getByRole('button', { name: 'Edit' })).toBeInTheDocument();
+    expect(screen.getByRole('button', { name: 'Delete' })).toBeInTheDocument();
+  });
diff --git a/src/features/Connectors/ConnectorDetail/index.tsx b/src/features/Connectors/ConnectorDetail/index.tsx
index aaffc7a6..e6bc189a 100644
--- a/src/features/Connectors/ConnectorDetail/index.tsx
+++ b/src/features/Connectors/ConnectorDetail/index.tsx
@@ -4 +4,3 @@ import { Button, confirmModal, toast } from '@lobehub/ui/base-ui';
-import { PencilIcon, RefreshCwIcon, Trash2 } from 'lucide-react';
+import { useSize } from 'ahooks';
+import { createStaticStyles } from 'antd-style';
+import { PencilIcon, RefreshCwIcon, Trash2, Unplug } from 'lucide-react';
@@ -6 +8 @@ import type { ReactNode } from 'react';
-import { memo, useCallback, useState } from 'react';
+import { memo, useCallback, useRef, useState } from 'react';
@@ -18,0 +21,48 @@ import ToolPermissionGroup from './ToolPermissionGroup';
+const COMPACT_HEADER_WIDTH = 760;
+
+const styles = createStaticStyles(({ css, cssVar }) => ({
+  actionButton: css`
+    display: inline-flex;
+    flex-shrink: 0;
+    align-items: center;
+    justify-content: center;
+
+    line-height: 1;
+  `,
+  actionLabel: css`
+    white-space: nowrap;
+  `,
+  actions: css`
+    display: flex;
+    flex-shrink: 0;
+    flex-wrap: wrap;
+    gap: 8px;
+    align-items: center;
+    justify-content: flex-end;
+
+    max-width: 100%;
+  `,
+  header: css`
+    display: flex;
+    gap: 12px;
+    align-items: center;
+    justify-content: space-between;
+
+    min-height: 42px;
+    padding-block: 12px;
+    padding-inline: 16px;
+    border-block-end: 1px solid ${cssVar.colorBorderSecondary};
+  `,
+  headerTitle: css`
+    overflow: hidden;
+    flex: 1 1 auto;
+    min-width: 0;
+    font-size: 14px;
+    font-weight: 500;
+    line-height: 1.5;
+    color: ${cssVar.colorText};
+    text-overflow: ellipsis;
+    white-space: nowrap;
+  `,
+}));
+
@@ -56,0 +107,2 @@ const ConnectorDetail = memo<ConnectorDetailProps>(
+    const headerRef = useRef<HTMLDivElement | null>(null);
+    const headerSize = useSize(headerRef);
@@ -186,0 +239,31 @@ const ConnectorDetail = memo<ConnectorDetailProps>(
+    const isCompactHeader = (headerSize?.width ?? Number.POSITIVE_INFINITY) < COMPACT_HEADER_WIDTH;
+
+    const renderCompactableButton = ({
+      danger,
+      disabled,
+      icon,
+      label,
+      loading,
+      onClick,
+    }: {
+      danger?: boolean;
+      disabled?: boolean;
+      icon: ReactNode;
+      label: string;
+      loading?: boolean;
+      onClick: () => void | Promise<void>;
+    }) => (
+      <Button
+        aria-label={label}
+        className={styles.actionButton}
+        danger={danger}
+        disabled={disabled}
+        icon={icon}
+        loading={loading}
+        size="small"
+        title={isCompactHeader ? label : undefined}
+        onClick={onClick}
+      >
+        {!isCompactHeader && <span className={styles.actionLabel}>{label}</span>}
+      </Button>
+    );
@@ -216,14 +299,3 @@ const ConnectorDetail = memo<ConnectorDetailProps>(
-        <div
-          style={{
-            alignItems: 'center',
-            borderBlockEnd: '1px solid var(--ant-color-border-secondary)',
-            display: 'flex',
-            flexShrink: 0,
-            gap: 8,
-            height: 42,
-            justifyContent: 'space-between',
-            paddingInline: 16,
-          }}
-        >
-          <div style={{ fontSize: 14, fontWeight: 500 }}>{connectorName}</div>
-          <div style={{ display: 'flex', gap: 8 }}>
+        <div ref={headerRef} className={styles.header}>
+          <div className={styles.headerTitle}>{connectorName}</div>
+          <div className={styles.actions}>
@@ -248,9 +320,7 @@ const ConnectorDetail = memo<ConnectorDetailProps>(
-              <Button
-                disabled={!canSync}
-                icon={<RefreshCwIcon size={14} />}
-                loading={syncing}
-                size="small"
-                onClick={handleSync}
-              >
-                {syncLabel}
-              </Button>
+              {renderCompactableButton({
+                disabled: !canSync,
+                icon: <RefreshCwIcon size={14} />,
+                label: syncLabel,
+                loading: syncing,
+                onClick: handleSync,
+              })}
@@ -261,8 +331,6 @@ const ConnectorDetail = memo<ConnectorDetailProps>(
-                <Button
-                  disabled={!canManage}
-                  icon={<PencilIcon size={14} />}
-                  size="small"
-                  onClick={() => setCustomModalOpen(true)}
-                >
-                  {t('connector.edit', 'Edit')}
-                </Button>
+                {renderCompactableButton({
+                  disabled: !canManage,
+                  icon: <PencilIcon size={14} />,
+                  label: t('connector.edit', 'Edit'),
+                  onClick: () => setCustomModalOpen(true),
+                })}
@@ -279,5 +347,6 @@ const ConnectorDetail = memo<ConnectorDetailProps>(
-                      <Button
-                        danger
-                        disabled={!canManage}
-                        size="small"
-                        onClick={async () => {
+                      {renderCompactableButton({
+                        danger: true,
+                        disabled: !canManage,
+                        icon: <Unplug size={14} />,
+                        label: t('connector.disconnect', 'Disconnect'),
+                        onClick: async () => {
@@ -289,4 +358,2 @@ const ConnectorDetail = memo<ConnectorDetailProps>(
-                        }}
-                      >
-                        {t('connector.disconnect', 'Disconnect')}
-                      </Button>
+                        },
+                      })}
@@ -295,6 +362,6 @@ const ConnectorDetail = memo<ConnectorDetailProps>(
-                      <Button
-                        danger
-                        disabled={!canManage}
-                        icon={<Trash2 size={14} />}
-                        size="small"
-                        onClick={() => {
+                      {renderCompactableButton({
+                        danger: true,
+                        disabled: !canManage,
+                        icon: <Trash2 size={14} />,
+                        label: t('connector.delete', 'Delete'),
+                        onClick: () => {
@@ -314,4 +381,2 @@ const ConnectorDetail = memo<ConnectorDetailProps>(
-                        }}
-                      >
-                        {t('connector.delete', 'Delete')}
-                      </Button>
+                        },
+                      })}
@@ -324,9 +389,7 @@ const ConnectorDetail = memo<ConnectorDetailProps>(
-                    <Button
-                      danger
-                      disabled={!canManage}
-                      icon={<Trash2 size={14} />}
-                      size="small"
-                      onClick={handleUninstall}
-                    >
-                      {t('connector.uninstall', 'Uninstall')}
-                    </Button>
+                    {renderCompactableButton({
+                      danger: true,
+                      disabled: !canManage,
+                      icon: <Trash2 size={14} />,
+                      label: t('connector.uninstall', 'Uninstall'),
+                      onClick: handleUninstall,
+                    })}
